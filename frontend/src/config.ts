// Optional Google OAuth client ID. When unset, the "Sign in with Google"
// button is hidden and only email/password auth is available.
export const GOOGLE_CLIENT_ID = import.meta.env.VITE_GOOGLE_CLIENT_ID
